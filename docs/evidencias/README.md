# Evidencia de funcionamiento

La captura `funcionamiento-frontend.png` corresponde a la aplicación Angular ejecutándose en `http://localhost:4200` y consumiendo la API REST conectada a MongoDB local.

En la tabla se observan los empleados persistidos y las acciones de edición y eliminación proporcionadas por el módulo CRUD.
